# UI (`src/ui/`)

All UI uses `bevy_egui` (not `bevy_ui`, except specific overlays).

- **Right-click context menu (`orders.rs`):** Three sections — tile actions (Move/Mine/Gather/Dig Down/Deconstruct/Build), entities on tile (Attack / Pick up corpse), ground item stacks (Pick up: Nx Good). UI is **event-only**: `right_click_context_menu_system` translates the chosen action into a `PlayerCommand` and emits `PlayerCommandEvent` (`simulation/player_command.rs`); sim attaches `Commanded` and dispatches. **Multi-select broadcast:** the menu renders against `SelectedEntity` (focus) but dispatch broadcasts to every non-drafted player-faction worker in `SelectedEntities` — drafted units stay under `military_right_click_system`. Adding an order: new `PlayerCommand` variant, arm in `dispatch_one`, arm in `player_command_lifecycle_system` (terminal-state detector), UI button emits the event.
  - **Vehicle Yard ▸ submenu** — right-click a player-faction `VehicleYard` lists every stock design as `Assemble <name>` → faction-level `QueueVehicle`.
  - **Vehicle ▸ submenu** (Phase 5) — right-click a player-faction `Vehicle` (or select one and click a destination tile) lists Move / Load / Unload / Assign Crew / Hitch / Unhitch / Salvage (an overturned hulk shows only Right / Salvage). `build_vehicle_ops` builds the list; each entry emits faction-level `PlayerCommand::VehicleOrder { vehicle, kind }` (`MenuAction::VehicleAct`). Selecting a vehicle opens a vehicle-only menu (the member-gate is widened to accept a selected player vehicle).
- **Vehicle designer (`vehicle_designer.rs`):** `egui::Window` toggled by the HUD "Vehicles" button (`VehicleDesignerState` resource) — a v2 three-column workbench over the `10×8×6` cell grid. Left column: `EditMode::{Cell, Module}` toggle; cell mode = part palette + variant `ComboBox` + material `ComboBox`; module mode = weapon-module list + rotation control + footprint preview. Centre: Z-layer tabs + the `10×8` slice grid (left-click places a part cell / stamps a module, right-click clears a cell / removes a whole module). Right: live `derive_stats` / `validate_grid` / `design_bill`, a "What this vehicle can do" summary, a tech-gate line, **plus an inset "Preview" canvas** painted via egui's `Painter` from the shared `entity_sprites::vehicle_sprite_plan(design, heading)` helper — same composition the spawned vehicle renders, with `state.preview_heading` (N/E/S/W) buttons rotating the in-plane offsets. Hover text on every part / variant / module / material / stat / validation error / occupied cell. Queue button disabled when the design is invalid **or** the player faction lacks a required tech; emits `PlayerCommandEvent { actors: [], command: QueueCustomVehicle { name, grid, purpose, required_animals } }` — event-only; the sim derives `tech_gates`, registers the design, and enqueues it. A "Saved & proposed designs" `CollapsingHeader` lists player-faction-authored designs (in-session customs + `vehicle_ai_design_proposal_system` proposals — `author_faction.is_some()`) AND on-disk `user_*.ron` designs (`from_user_file == true`, stamped by `load_vehicle_assets` when the filename has the `user_` prefix) with a Load button. **Save to disk** writes the current design as `assets/data/vehicles/user_<slug>.ron` via `vehicle::save_custom_design`; the loader picks it up on next game start and surfaces it in the same list. **Debug-only "Test Drive" button** (`cfg!(debug_assertions)`-gated) emits `PlayerCommand::DebugSpawnTestVehicle` — see "Manual drive" below.
- **Manual drive (`manual_drive.rs`):** debug Test-Drive control loop. `ManualDriveState { active: Option<Entity>, last_status }` (resource in `simulation::vehicle`) marks the player-driven vehicle; `manual_drive_input_system` (Update set) reads `just_pressed` for W/Up = forward step, Q/E = forward-left/right diagonal, A/D/Left/Right = turn in place, S/Down = clear in-flight `VehiclePathFollow`, Esc = release. Egui keyboard focus suppresses every key. Movement keys drop while `VehiclePathFollow` is in flight (tap-to-step, no buffering). One-step paths come from `vehicle::plan_manual_step` (same `cell_ok` + heading rules as `plan_vehicle_route`, so rollover / speed flow through `vehicle_movement_system` identically). `rendering::camera::camera_input_system` short-circuits WASD pan while `manual_drive.active.is_some()` so the keys steer instead of pan. Coexists with the right-click `VehicleOrderKind::MoveTo` flow — manual taps just feed the same `VehiclePathFollow` slot.
- **Inspector — Vehicle section (`inspector.rs`):** when the selected entity is a `Vehicle`, the panel renders a single "Vehicle" `CollapsingHeader` (design name, state, footprint w×d×h, empty/payload mass, road/off-road speed, stability, turn radius, cargo manifest, driver, hitched-animal count, route progress, overturned warning) and returns early — the Person sections are skipped. Backed by `VehicleInspectorParams` (nested inside `TaskDisplayParams` to keep the panel under Bevy's param ceiling).
- **Inspector (`inspector.rs`):** Drop / unequip / equip queue actions via `PendingInspectorAction`; `inspector_action_system` executes after panel render. `valid_equip_slots(good)` (`../simulation/items.rs`) maps goods to slots. Knowledge section lists Learned/Aware sets with Lecture/Encode (Learned) and Read (Aware with matching tablet) buttons; "In progress" sub-section lists `study_progress`. **Task & State** reads `MethodHistory` + `SimClock`: `Method:` falls back to `(direct dispatch — no HTN method)` for bureaucrats / traders / player orders, or `(none — see HTN history)` when fully idle; idle agents show ticks_idle + `last_goal_eval_tick`. **Profession line** inlines apprenticeship: `Profession: Apprentice  apprentice 4/30 d (13%)` plus mentor entity index; masters show `mentoring #N`. **Skills section** prints `cur / peak P (floor F)` via `skills::skill_floor(peak)` (shared with `skill_decay_system`); mastered (peak ≥ `SKILL_MASTERY_LINE`) render green. **Wage & Labor section** (between Stats and Knowledge): 24-hour Earnings rollup + ring total + last entry; per-profession EV table (`Farmer / Hunter / Crafter / Bureaucrat / Trader` — current highlighted yellow, zero-EV greyed) using `capital::capital_factor`; own-faction `wage_signal` top-6 by EMA/day; cross-faction `PerceivedFactionWages` top-6 with observation-age. Params bundled into `WageInspectorParams` SystemParam (peaks / earnings / perceived / apprenticeship / household + `WorkshopOwnership` + `PlotIndex` + `Plot` query) to stay under Bevy's per-tuple ceiling.
- **Tech panel (`tech_panel.rs`):** Per-tech icon mirrors community `AdoptionStage` — ◆ Institutionalized / ✓ Adopted / ● Practiced / ◐ Demonstrated / ◉ Rumored / ◎ Discoverable / ○ Locked. Hover shows adoption stage, chief's Learned/Aware (separately) and per-tech member tally.
- **Activity log (`activity_log.rs`):** Bottom-right egui panel; `ActivityLogEvent { tick, actor, faction_id, kind }`. Kinds: `Constructed`, `Crafted`, `TechInsight`, `TechDiscovered`, `TechAdopted` / `TechInstitutionalized` (from `derive_tech_adoption_system`), `RegionSettled`, `Taught`, `Read`, `CampMoved`, `Sedentarized`, `WagePaid`, plus labor-market entries: `ProfessionChanged { from, to }` emitted centrally by `profession_choice::profession_change_log_system` (Economy, observes `Changed<Profession>` via `LastSeenProfession` shadow — runs after profession-mutation systems so same-tick chains collapse to one entry); `ApprenticeshipStarted { mentor }` from `chief_craft_assignment_system`; `ApprenticeshipGraduated` from `apprentice_progress_system`. Centralised emitter suppresses `None↔Apprentice` / `Apprentice→Crafter` to avoid double-logging. Filtered to player faction; cap 16.
- **Spawn-select (`spawn_select.rs`):** Full-screen biome map rendered at `WORLD_MAP_OVERSAMPLE=4` × climate-cell density via per-pixel `biome::classify_surface_at_tile` (surface-biome warp layer, see `world/CLAUDE.md`), with elevation-driven hillshade (`0.55 + 0.95*elev_f`). Texture uploaded with `egui::TextureOptions::LINEAR` so feathered biome borders read smooth at the 10× display scale (1px grid / river overlays soften slightly). Two grid overlays: faint climate sub-grid (every 4 cells) + bold mega-chunk grid. Hover paints a yellow centre dot on the hovered cell and shows a tooltip: mega-chunk coords, centre tile, exclusive tile bounds, dominant biome (8×8 surface-classify tile-grid sample over the megachunk so the label matches the rendered image), centre elevation, min/mean/max elevation (4×4 sample via `sample_elevation_stats`), avg fertility (8×8 climate-derived sample via `region::average_fertility_in_megachunk`), temperature, "Player home stays inside this cell." reminder. Click a habitable mega-chunk → `PendingSpawn`; gameplay home is constrained to that cell by `region::pick_player_home_in_megachunk`. Ocean/Mountain non-habitable. Side panel: era / pop / economy / lifestyle controls + **World** section with editable `Seed:` + Apply/Reroll buttons firing `RegenerateWorldRequest` (handled by `world::regenerate_world_system`), which rebuilds `Globe` + `WorldGen` and clears `SpawnSelectTexture` / `WorldMapTexture` caches.
- **World-map switcher (`world_map.rs`):** Settled mega-chunks outlined (yellow=player, red=other). Renders at `WORLD_MAP_OVERSAMPLE=4` via per-pixel `biome::classify_surface_at_tile`, uploaded LINEAR — preview matches the surface-biome layer chunk-gen stamps (organic feathered borders, ecotone speckle). Click bookmarks current camera, then jumps to target's `camera_bookmark`. Tab toggles map. **Fertility overlay (toggleable):** `WorldMapView.show_fertility` checkbox tints every mega-chunk by `region::average_fertility_in_megachunk` (brown→tan→green ramp, 65% alpha blend) before the river polyline pass so coasts/rivers stay legible; texture cache keys on the toggle so flipping it triggers exactly one rebuild. Hover anywhere on the map shows a tooltip with mega-chunk coords, centre biome (surface-classify at the megachunk centre), and avg fertility (climate-derived, chunk-independent — works for unloaded regions).
- **Speed controls (`hud.rs` + `simulation/speed.rs`):** HUD speed buttons (`⏸ / 1× / 2× / 5×`) write `GameSpeed.current`, which a PreUpdate sync system mirrors onto `Time<Virtual>::set_relative_speed / pause`. Keyboard shortcuts (`Space` toggles pause; `1/2/3` pick `Normal/Fast/VeryFast`) are gated on `EguiContexts::wants_keyboard_input` so typing into a panel doesn't hijack them. The debug panel's "Sim Timing" section shows ticks/frame + EMA tick CPU + worst-tick window (60-sample), reddens when `avg > preset.budget_ms_per_tick()`.
- **Muster button (`hud.rs`):** Emits `PlayerCommandEvent { actors: <player Hunters>, command: Muster }`; dispatch inserts `Drafted`, clears plan/reservations, removes `Carrying`. Rally point set separately via right-click. `apply_draft_toggle_system` (HUD button + `R`) emits `Muster` or `Disband` based on whether any selected unit is undrafted. Orthogonal to chief muster (uses `HuntOrder::Hunt::mustered`, not `Drafted`).
- **Pack/Pitch Camp (`hud.rs` + `orders.rs`):** Mobile (nomadic) player factions show `Pack Camp` when `camp_state == Pitched` (emits `PackCamp` on chief). When `Packed`, HUD reads "Mobile (right-click → Pitch)" and right-click adds `Pitch Camp Here` on passable tiles ≥ `MIN_PITCH_DISTANCE` from packed centroid (emits `PitchCamp { tile, z }`). Between Pack and Pitch, members default to "Awaiting Orders" (strict `PackedMigrationAutonomy::Hold`); HUD's `Autonomy: Hold/Forage` toggle (mirrored in Migration panel) flips to `Forage` for autonomous food/sleep/social/scout/play. `PackCamp` resets to `Hold`.
